This is my nixconfig

the vision is to strucute and modularize my nix config for ease or use, reproducibility and to keep everyting in one place and setup from one place and in a structured manner

the hosts directiry is for different machines, the common containt things and general configs that every machine should have, like boot settings, locale, console fonts, etc.etc..., then there is "jinnnn" (name was suggested by someone) whicn my current one and only laptop that i daily drive, this contains configs and stuff tied to this specific laptop, it only conatins thinsg that not general (uncommon). This hosts dir contains things that that tied to the operating system rather than the user, like enabling network, and other system servies

then there is users dir, in which common conatins things and general stuff that every user on any of my macines must have like popular programs like git, curl etc.etc.. but contains no specific configs for them (or may contains some basic ones, not too specific like enabling relative number on nvim etc.)) . Then comes my user, meher, which contains MY setttings, configs and my desired apps that my user on any machine must have, this also contains the configs for each of those apps


i think that is it for now.
